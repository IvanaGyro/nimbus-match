name: Weekly Font Match Release
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
    inputs:
      force_build:
        description: Build even when input identity is unchanged
        type: boolean
        default: false
      family:
        description: A single family produces development artifacts only
        type: choice
        options: [all, nimbus-match, termes-match]
        default: all
concurrency:
  group: font-match-release
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  prepare:
    runs-on: windows-latest
    outputs:
      should_build: ${{ steps.prepare.outputs.should_build }}
      version: ${{ steps.prepare.outputs.version }}
      families: ${{ steps.prepare.outputs.families }}
      publish: ${{ steps.prepare.outputs.publish }}
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.3
        with:
          pixi-version: v0.73.0
      - id: prepare
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SELECTED_FAMILY: ${{ inputs.family || 'all' }}
          FORCE_BUILD: ${{ inputs.force_build }}
        run: |
          $arguments = @('prepare', '--family', $env:SELECTED_FAMILY)
          if ($env:FORCE_BUILD -eq 'true') { $arguments += '--force' }
          pixi run python -m font_match.release @arguments
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
      - uses: actions/upload-artifact@v4
        if: steps.prepare.outputs.should_build == 'true'
        with:
          name: resolved-inputs
          path: |
            build_temp/inputs.json
            build_temp/archives/*.zip
          if-no-files-found: error
  build:
    needs: prepare
    if: needs.prepare.outputs.should_build == 'true'
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        family: ${{ fromJSON(needs.prepare.outputs.families) }}
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.3
        with:
          pixi-version: v0.73.0
      - uses: actions/download-artifact@v4
        with:
          name: resolved-inputs
          path: build_temp
      - run: pixi run font-match build --family ${{ matrix.family }} --version ${{ needs.prepare.outputs.version }}
      - run: pixi run pytest --junitxml=build_temp/tests.xml
        env:
          FONT_MATCH_FAMILY: ${{ matrix.family }}
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: diagnostics-${{ matrix.family }}
          path: build_temp/tests.xml
          if-no-files-found: ignore
      - run: pixi run font-match verify --family ${{ matrix.family }}
      - uses: actions/upload-artifact@v4
        with:
          name: fonts-${{ matrix.family }}
          path: dist/${{ matrix.family }}/
          if-no-files-found: error
  publish:
    needs: [prepare, build]
    if: needs.prepare.outputs.publish == 'true'
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.3
        with:
          pixi-version: v0.73.0
      - uses: actions/download-artifact@v4
        with:
          name: resolved-inputs
          path: build_temp
      - uses: actions/download-artifact@v4
        with:
          name: fonts-nimbus-match
          path: dist/nimbus-match
      - uses: actions/download-artifact@v4
        with:
          name: fonts-termes-match
          path: dist/termes-match
      - run: pixi run python -m font_match.release publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
